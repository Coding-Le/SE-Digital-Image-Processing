MAIN函数可以一次性完成所有采样和量化的工作，也就是依次调用quantize.m scale_size.m scale_size3.m这几个函数。

其中scale_size.m函数是成倍放大和缩小图像，由于采用的是矩阵相乘的形式，所以对于非整数倍的采样无能为力。

scale_size3.m函数既可以成倍放大和缩小图像，也可以按任意比例放大和缩小图像，采用的是双线性插值的方法。

quantize.m函数是对图像进行量化，将灰度级别为256的图像量化为128、32、8这样灰度级别的图像。